# Convex deployment — copy to .env.local (gitignored) or let `npx convex dev` write it.
# The dev deployment URL is what the iOS client (P3 ConvexService) connects to.
CONVEX_URL=https://<your-deployment>.convex.cloud
