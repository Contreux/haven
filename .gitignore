# Xcode / SwiftPM
.build/
DerivedData/
*.xcodeproj
*.xcworkspace
.swiftpm/
xcuserdata/

# Node / Convex (P2)
node_modules/
.convex/
.env.local
# Stray transpiled JS next to convex/*.ts sources (keep convex/_generated/*.js)
/convex/*.js

# Maestro UI-test screenshot output (root-level *.png; design assets live in subdirs)
/*.png
.maestro/

# macOS
.DS_Store
